The top (table of processes) command is a real-time system monitoring utility built into most Linux distributions. 
It provides a dynamic, continuously updating view of the system's performance, including CPU and memory usage, and a list of all active processes.

The 'minitopimplementation' code tries to replicate the TOP command and the 'ProjectSummary.pdf' explains the code in detail
